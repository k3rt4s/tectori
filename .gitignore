# Operating system files
Thumbs.db
.DS_Store

# Local editor and build artifacts
.vscode/
node_modules/
dist/

# Generated data belongs outside the repository, in whatever directory the
# person running the build passes to --out. Nothing here writes into the tree
# except a rebuild of docs/, which is tracked output.
data/
logs/

# Local secrets
.env
.env.*
!.env.example
